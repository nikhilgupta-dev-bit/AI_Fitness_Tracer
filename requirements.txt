# Web framework
Flask==3.1.3
flask-socketio==5.5.1
python-socketio==5.12.1
python-engineio==4.11.2

# WSGI + async worker (required for Socket.IO WebSocket transport)
gunicorn==23.0.0
eventlet==0.39.1

# Computer vision + pose estimation
# Let pip resolve sub-dependencies (absl-py, protobuf, flatbuffers etc.)
mediapipe==0.10.32
opencv-python-headless
numpy
