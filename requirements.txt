# Web framework
Flask==3.1.3
flask-socketio==5.5.1
python-socketio==5.12.1
python-engineio==4.11.2

# WSGI + async worker (required for Socket.IO WebSocket transport)
gunicorn==23.0.0
eventlet==0.39.1

# Computer vision + pose estimation
# Pinned version of mediapipe for stability
mediapipe==0.10.32

# Force headless opencv to avoid libxcb.so.1 errors on Linux servers
opencv-python-headless==4.10.0.84
numpy==1.26.4
